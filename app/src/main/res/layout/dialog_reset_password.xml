<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="@string/label_old_password"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <seaice.app.groupcontact.view.PasswordView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/enter_old_password"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginBottom="8dp"
        android:inputType="numberPassword"
        android:maxLength="6"
        android:paddingBottom="8dp"
        android:paddingTop="8dp"
        app:borderColor="@color/passwordBorder"
        app:passwordColor="@color/passwordColor"
        app:passwordWidth="@dimen/custom_ev_password_width" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="@string/label_new_password"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <seaice.app.groupcontact.view.PasswordView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/enter_new_password"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:inputType="numberPassword"
        android:maxLength="6"
        android:paddingBottom="8dp"
        android:paddingTop="8dp"
        app:borderColor="@color/passwordBorder"
        app:passwordColor="@color/passwordColor"
        app:passwordWidth="@dimen/custom_ev_password_width" />

</LinearLayout>